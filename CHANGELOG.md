# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/).

## [Unreleased]

### Added
- Monorepo setup with pnpm workspaces (`server`, `cli`, `shared`)
- Mastra v1.13 server with `assistant` agent (Claude Sonnet)
- LibSQL-backed conversation memory
- CLI with one-shot print mode (`openzeus -p "query"`)
- SSE streaming from server to CLI
- Shared TypeScript types and API protocol definitions
- ZEUS.md project instructions file
